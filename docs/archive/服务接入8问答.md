# 跳舞兰花卉智能体 · 服务接入 8 问

> 整理日期：2026-09-04
> 来源：README.md / DEPLOY.md / FRONTEND_CONTRACT.md 与源码（main.py、backend/routers/chat.py、agent/engine/ui_protocol.py）逐字段核对
> 适用对象：想把这套智能体代码跑成 HTTP 服务并接入的第三方 / 业务平台方

---

## 1️⃣ 启动命令和运行环境

```bash
# Docker 生产部署（推荐，镜像内已装中文字体 wqy-microhei + 阿里云 apt 源）
docker compose up -d --build          # 含 postgres + agent，可选 nginx（profile）

# 改代码后必须重建 agent 镜像才生效（restart 不生效）
docker compose up -d --build agent

# 本地开发（Python 3.12）
pip install -r requirements.txt
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

运行环境要求：
- Python 3.12（本地）/ Docker（生产）
- 内部库 PostgreSQL 16（生产必填）
- 可选对接外部业务只读库（`PLATFORM_DB_<SOURCE_ID>_URL`）与下单转发接口（`PLATFORM_ORDER_API_URL`）

---

## 2️⃣ 聊天接口地址

Base URL：生产 `https://你的域名`（如 `https://api.tiaowulan.com`），本地 `http://localhost:8000`

```text
POST /chat          同步对话，返回完整结构化响应（一次请求一轮回答）
POST /chat/stream   SSE 流式对话（事件：tool_call / text / card / done / error）
```

---

## 3️⃣ 请求 / 返回 JSON 格式

**请求体 `POST /chat`**

```jsonc
{
  "message": "推荐一束生日花",
  "user_id": "wxmini_<32位hex>",   // 必填，多平台接入时须等于 /auth/token 返回的派生 user_id
  "session_id": null,               // 可选，续聊传上次返回的 session_id
  "location": null,                 // 可选，结构化地点信息（对象）
  "shop_id": null                   // 可选，店铺锁定模式：进入智能体第一轮传店铺 ID
}
```

**返回体 `POST /chat`（统一 ChatResponse）**

```jsonc
{
  "user_id": "wxmini_<32位hex>",
  "reply": "我为你推荐了…",          // 必渲染文本
  "ui": "plan_card",               // 渲染类型：text / dialog_options / plan_card /
                                   // shop_card / order_card / pay_jump /
                                   // image_task / greeting_card（共 8 种）
  "data": { ... },                 // 与 ui 一一对应的结构化数据（见第 6 问）
  "action": { "type": "...", "payload": {}, "required_capabilities": [], "fallback": "" },
  "tool_calls": [],                // 本轮工具调用记录（name/arguments/result/status）
  "session_id": "conv_xxx",        // 会话 ID，续聊时回传
  "stage": "plan_confirm"          // 会话阶段，便于前端感知进度
}
```

---

## 4️⃣ 是否支持会话 ID

**支持。**

- 请求带 `session_id` → 续聊同一会话（智能体会记住上下文与该会话绑定的 shop_id）
- 不带 → 自动新建会话，并在返回体的 `session_id` 字段回传

相关接口：

```text
GET    /conversations                    会话列表
GET    /conversations/{id}/messages      某会话消息历史
POST   /conversations                    手动建会话（body: user_id / title / shop_id）
POST   /chat/reset                       重置会话（body: user_id / session_id）
```

---

## 5️⃣ 如何传入 shopId

`POST /chat` 请求体顶层字段 **`shop_id`**（如 `"shop_id": "S001"`）。

- **在进入智能体的第一轮就传** → 进入「店铺锁定模式」：该 `shop_id` 绑定整个会话，之后同 `session_id` 的轮次可不重复传；
- 锁定后，店铺推荐、花束方案（DIY 原料）、下单**全部限定在该店铺**，智能体不会推荐别店、下单时越店会被拒绝；
- 想脱离锁定需在会话层重置（新 session_id 或调用会话重置接口，不传 shop_id）。

---

## 6️⃣ 推荐商品时返回什么字段

**花束方案（`ui = "plan_card"`，`data.plans[]` 每项）**

| 字段 | 类型 | 说明 |
|---|---|---|
| `plan_id` | string | 方案 ID |
| `name` | string | 方案名（如"生日玫瑰花束"） |
| `price` | number | 价格（元，float） |
| `desc` | string | 方案描述 |
| `effect_image_url` | string | 效果图 URL |
| `merchant_name` | string | 商家 / 店铺名 |

**店铺（`ui = "shop_card"`，`data.shops[]` 每项）**

| 字段 | 类型 | 说明 |
|---|---|---|
| `shop_id` | string | 店铺 ID |
| `name` | string | 店铺名 |
| `distance_km` | number | 距离（km） |
| `price_range` | string | 价格区间 |
| `rating` | number | 评分（0-5） |

---

## 7️⃣ 模型密钥通过什么环境变量填写

`.env` 文件（生产用 `.env.production`），二选一填一组 LLM：

```bash
# 方式一：OpenAI 兼容接口
LLM_API_KEY=sk-xxx
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# 方式二：腾讯云 hy 大模型（默认推荐）
HY_API_KEY=sk-xxx
HY_BASE_URL=https://tokenhub.tencentmaas.com/v1/responses
HY_LLM_MODEL=hy3
```

其它相关密钥：

```bash
# 图像生成：IMAGE_PROVIDER 仅 mock / hy 两种已实现，其余为预留
IMAGE_PROVIDER=hy          # mock=本地占位图 | hy=腾讯云混元生图
IMAGE_API_KEY=sk-xxx

# 多平台接入鉴权（platform_id=key 逗号分隔）
PLATFORM_API_KEYS=wxmini=key1,h5app=key2

# 业务平台方：外部只读库连接 + 下单转发
PLATFORM_DB_WXMINI_URL=postgresql://readonly:xxx@host:5432/db
# 或 MySQL（mysql://...，如 PLATFORM_DB_WXMINI_URL=mysql://readonly:xxx@host:3306/flower_shop）
PLATFORM_ORDER_API_URL=https://platform.example.com/api/order
PLATFORM_ORDER_API_KEY=xxx

# 生产必配：JWT_SECRET（≥32 位随机串）
JWT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## 8️⃣ 健康检查接口

```bash
GET /health
```

```jsonc
// 200 响应
{
  "status": "ok",
  "service": "flora-agent",
  "version": "1.0.0"
}
```

---

### 附：鉴权与完整接口一览

非匿名环境调用 `/chat` 前需先换 token：

```text
POST /auth/token      body: { "api_key": "<平台 key>", "external_user_id": "<平台用户 ID>" }
                     → 返回派生 user_id 与 JWT，/chat 时 Header 带 Authorization: Bearer <JWT>
POST /auth/wx-login   微信小程序登录（code 换身份）
POST /auth/anonymous  匿名登录（仅开发环境）
```

完整路由清单：`/health`、`/auth/*`、`/chat`、`/chat/stream`、`/chat/reset`、`/conversations`、`/conversations/{id}/messages`、`/tasks/{task_id}`、`/ui-contract`

更细的接入说明见仓库内《接入说明-花艺智能体API.md》与 FRONTEND_CONTRACT.md。
