# 花艺智能体 - Python 依赖
# 核心
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# LLM
openai>=1.6.0
httpx>=0.25.0

# 数据库：生产使用 PostgreSQL；外部只读数据源同时支持 PostgreSQL / MySQL
psycopg[binary]>=3.2.0
pymysql>=1.1.0

# 认证与 MCP
PyJWT>=2.8.0
mcp[cli]>=1.12.4,<2.0.0

# 电子贺卡模板合成（中文排版依赖系统中文字体，Docker 已装 fonts-wqy-microhei）
pillow>=10.0.0
