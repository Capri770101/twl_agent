# 智能体版本记录

## 未发布 — 语音能力与贺卡视觉升级（2026-09-24）

### 语音（TTS / ASR）

- 新增 `/speech/tts`（文本转语音）与 `/speech/transcribe`（语音转文本），走百炼原生
  `qwen3-tts-flash` / `qwen3-asr-flash`，复用 `LLM_API_KEY`，与对话、生图同厂商。
- `/chat` 响应新增 `speech_text` 字段：按 UI 类型确定性生成「结果一句话 + 下一步操作」，
  语音只念引导不念全文（零 LLM 成本）；`SPEECH_ENABLED=false` 时为空串。
- TTS 结果按 `(model, voice, text)` 哈希缓存落 `data/generated/speech_*.wav`，同文案不重复合成。
- 端点要求 Bearer + user_id 归属校验、按用户限流、音频体积/MIME 双重校验；上游音频 URL 下载前做 SSRF 校验。
- 语音用量并入现有监控面板：新增 `speech_logs` 表 + `record_speech_call` 埋点 +
  `GET /api/metrics/speech`，面板加「语音用量」卡片（TTS 字符/缓存命中、ASR 秒数、错误、延迟）。
- 新增 `scripts/try_speech.py` 试用脚本：无需前端即可 TTS 试听、ASR 转写、回环验证。
- 前端录音需 HTTPS（手机浏览器 `getUserMedia` 安全上下文限制），智能体端点与传输层无关，已可用。

### 贺卡视觉

- `/greetings/render` 由同步 Pillow 模板合成改为**异步任务**：AI 生成花卉背景 + 服务端排版文字，
  响应给 `task_id`/`poll`，前端轮询取 `result_url`（契约同 image_task）。
- 解决两版实测问题：纯 AI 出图中文乱码、不像贺卡；纯 Pillow 合成千篇一律。
- 生图提示词强制花材集中在顶部/边缘、下半部留净底，禁止任何文字/水印。

### 修复

- `validate_plan_warnings` 单一花材约束回归：此前只比对主花名称，漏判「主花 + 配花/叶材」
  仍违反单一花材的情况；现一并检查 fillers/foliage。
- `tests/test_greeting_api.py` 过期用例随 render 异步化更新为断言任务契约。
- 本地全量 pytest 通过（系统 Python 3.12 + psycopg；flora-dev venv 的 pytest 仍损坏，未使用）。

## 未发布 — 真实评测后续修复（2026-09-22）

- 养护问句补充“多久换一次水”等自然表达识别。
- QA 空泛回复从检索成功结果中生成最低限度的可读养护回答。
- DIY 与商品/平台事实护栏解耦；系统内部取数与模型工具调用分开记录，避免评测误报。

## 1.3.0 — 2026-09-22

### 知识库

- 花材扩充至 78 条、养护 30 条、搭配 40 条、场景 32 条。
- 新增知识库质量门和维护文档；知识检索黄金集 `hit@5=1.00`、`MRR=0.9467`。
- 修复知识扩充带来的花材别名归一化和检索回归。

### 智能体路由与质量

- 生产开启养护工具范围和意图路由：独立养护、贺卡、生图、DIY 请求按意图限制工具集和 ReAct 轮数。
- 明确定制/单一花材请求不再暴露现成商品查询；商品推荐实体收窄到 `plan`。
- 增加知识问答实质回答兜底，避免只返回“请查看卡片”。
- DIY 方案增加确定性约束修正与阻断：排除花材、单一花材、支数和预算。
- 评测工具区分模型工具调用与系统内部调用，新增真实 HTTP 场景评测管道。

### 生图与视觉风格

- 店铺在售商品摘要提取风格、颜色和包装信号，增强生图 prompt。
- 包装 prompt 增加现代多层纸材、折角、丝带、礼盒、瓶插和老旧风格负面约束。
- 明确多图视觉参考暂未接入，当前失败回退文本生图。

### 贺卡

- 新增 `/greetings/draft` 个性化贺卡文案草稿接口。
- 新增 `/greetings/render` Pillow 贺卡渲染接口。
- 贺卡可读取当前会话方案上下文；宿主订单摘要由调用方提供，暂不自动读取订单或发送贺卡。

### 验证与发布

- 本地全量 pytest：801 passed。
- H5 `npm run build` 通过。
- 本版本通过 IP 访问生产服务验收；域名停用期间使用智能体 `49.232.49.176`、H5 `129.204.85.139`。

## 未发布 — QA/DIY 质量收口与包装评测（2026-09-22）

- 补齐此前缺失的 knowledge_fallback_reply 实现，仅提取成功检索的公开字段，并在最终清理后处理 QA 占位答复。
- 明确设计路由不再触发“必须查询平台商品”护栏。
- 新增五类包装生图评测定义；视觉质量仍需同题出图和人工评分，未声称已完成。
- 本地全量 801 项通过；尚未完成本批真实模型评测或生产发布。

## 未发布 — 现代包装视觉 prompt（2026-09-22）

- 将抽象包装名扩展为多层纸材、半透明材质、折角、丝带、礼盒结构、现代瓶插等可视化描述。
- 新增老旧单层纸、廉价塑料、大蝴蝶结、传统对称大花束等负面约束。
- 包装名称仍作为事实字段保留，花材和支数硬约束不变；未接入多图参考生图。
- 全量测试：799 passed（本地）。

## 未发布 — 真实评测回归修复（2026-09-22）

- 评测判定不再把“请查看下方卡片”当作养护问答成功回复。
- 知识检索后模型正文为空时，从检索结果生成最小可读养护回答。
- 明确定制 / 单一花材请求优先使用 DIY 工具集；商品推荐请求的实体 schema 收窄为 plan。
- 评测集 8 条在真实代理验收中仍有模型质量样本需继续观察；工具序列以服务端日志为准。

## 未发布 — 路由边界修复（2026-09-22）

- 明确 DIY 请求使用设计工具集，商品推荐的实体范围限制为 plan。
- 部署实体白名单和本轮范围交集为空时拒绝全部查询，不回退为无限制。
- 保留“定制并出图”的设计能力，识别“不要定制”和“不要效果图”。
- 真实评测拒绝空回复、养护占位回复和缺少 plans 的空方案卡。
- 本地全量 794 项通过；上线情况以实际发布记录为准，不能把健康检查当作内容质量验收。

## 未发布 — 知识库与贺卡本地验收（2026-09-22）

- 知识库扩充：花材 78、养护 30、搭配 40、场景 32；检索黄金集 hit@5=1.00、MRR=0.9467。
- 方案校验升级：确定性修复排除花材、单一花材和单主花支数；无法安全修复时标记 blocked，工具返回失败且不写入会话。
- 新增 `scripts/run_eval.py`，通过真实 HTTP 批量执行场景集并记录 UI、工具序列、耗时和报告。

- 新增 `/greetings/draft`：根据调用方商品摘要、客户表达、关系与场合生成可编辑草稿。
- 新增 `/greetings/render`：确认正文后渲染 PNG，复用 Bearer 身份、按用户限流和有限并发。
- 订单摘要仅为调用方输入，不自动查询/验证订单归属；无订单完成自动触发。接口见 docs/GREETING_API.md。

- 贺卡工具读取当前会话 selected_plan/latest_diy_plan；不查询真实订单。
- 知识库清单 v2 和 validate_knowledge.py：检查文件、JSON 结构、已有 ID 重复和部分域字段；颜色文件被校验不等于已接入检索。
- 知识库质量脚本纳入默认 pytest。
- 五套贺卡模板均验证真实 PNG 生成、解码和本地 HTTP 读取；会话上下文使用测试替身，正文由测试提供。
- 全量测试：789 passed（2026-09-22）；知识库脚本通过；8 个评测样本结构有效。
- 未验证：真实订单授权快照接入、客户意图驱动的独立文案生成、真实业务 API 静态挂载和视觉排版质量。
- 新增店铺风格画像：按店铺实时商品摘要提取风格/色彩/包装信号追加到生图 prompt；当前不传商品图片，失败回退旧 prompt。
- api.tiaowulan.com 已停用，本批只在本地验收，没有部署或访问该域名。

## 1.2.0 — 2026-09-21（演示候选）

- 建立 `evals/flower_scenarios.jsonl` 场景评测集与离线校验脚本。
- 新增保守意图路由：明显养护、贺卡、生图请求可限制工具集和最大 ReAct 轮数；默认关闭。
- 新增 `AGENT_INTENT_ROUTING_ENABLED` feature flag，生产候选验证前保持关闭。
- 收敛评测规范、工具边界和质量指标文档。

验证：全量 pytest 通过、评测集结构校验通过；先部署演示环境开启路由，再决定生产发布。

## 1.1.0 — 2026-09-21

- 纳入此前已上线的寒暄快速路径、无真实 DIY 方案的指定生图请求保护。
- 完整轮次历史预算（默认不裁剪）、精简终结工具描述。
- 新增独立养护工具范围开关 CARE_TOOL_SCOPE_ENABLED，默认关闭；代码发布不代表实验开关启用。
- 数据源访问增加执行取消检查。
- 补入上下文、取消/费用、生图绑定、寒暄和工具范围回归测试。
- 收录 DIY 交易讨论稿、小程序出图文档及历史优化计划；讨论稿不是上线功能。
- 交付文档补齐 README、DELIVERY 和 docs 文档地图；历史资料明确归档用途。
- 本次以 Git 提交归档并部署，发布目录记录 commit、源代码包和旧镜像。

验证：全量 pytest；演示和生产健康检查及对话冒烟。实际结果见服务器发布目录。
回退：使用发布目录记录的旧镜像重建 agent/agent-demo，配置和数据库不变。
# 2026-09-24 鲁棒性审查

- 修复生图错误被误判成功、无最终工具回复时任务信息丢失、latest_diy 引用旧成品。
- ASR 限量读取、试用脚本错误退出码、监控延迟加权；增加预览图回归测试。
- 更新联动审查文档，归档早期贺卡验收快照。
